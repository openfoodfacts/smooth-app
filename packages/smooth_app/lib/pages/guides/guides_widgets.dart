import 'package:flutter/material.dart';

class GuideShareFooter extends StatelessWidget {
  const GuideShareFooter({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
